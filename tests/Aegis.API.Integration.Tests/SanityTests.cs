using Xunit;

namespace Aegis.API.Integration.Tests;

// Placeholder so `dotnet test` has at least one test to run until real
// WebApplicationFactory/Testcontainers-based integration tests are written.
public class SanityTests
{
    [Fact]
    public void Sanity_AlwaysPasses()
    {
        Assert.True(true);
    }
}
